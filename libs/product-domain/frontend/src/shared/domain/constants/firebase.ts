export enum FIREBASE_ERROR_CODES {
  ALREADY_LINKED = 'auth/provider-already-linked',
  INVALID_VERIFICATION_CODE = 'auth/invalid-verification-code',
  VERIFICATION_CODE_EXPIRED = 'auth/code-expired',
}
