import { productDomainFrontend } from './product-domain/frontend';

describe('productDomainFrontend', () => {
  it('should work', () => {
    expect(productDomainFrontend()).toEqual('product-domain/frontend');
  });
});
