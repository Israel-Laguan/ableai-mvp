# bakcned

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bakcned` to build the library.

## Running unit tests

Run `nx test bakcned` to execute the unit tests via [Jest](https://jestjs.io).
