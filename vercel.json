{
  "version": 2,
  "builds": [
    {
      "src": "apps/auth-api/src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/auth-api/(.*)",
      "dest": "apps/auth-api/src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}